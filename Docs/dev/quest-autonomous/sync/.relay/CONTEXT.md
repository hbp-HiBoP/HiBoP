The approved source is `Docs/dev/quest-autonomous/sync/`. Its cross-task definition of done applies to every task: business-setter causal boundaries; distinct logical and attempt identities; one owner for mutable work; success and failure symmetry; non-perturbing telemetry; inactive-path and bounded-work guarantees; production-entry-point proof; honest unavailable evidence; and feature-to-Core assembly direction. Keep the branch compiling at each declared boundary. Run `Tools/check-assembly-dependencies.ps1` before Unity tests, `Tools/format-code.cmd` as a worker formatting step for changed C#, and `git diff --check` before handoff. Preserve the `Sync.Fast`, `Sync.Loopback`, `Sync.SceneFocused`, `Sync.Qualification`, and `Sync.Device` tiers and their execution-time reporting. The formatter changes files and is excluded from read-only checks.

Verification strategy: **unity-mcp**. The documented local endpoint is `http://127.0.0.1:8080/mcp`. Select the connected HiBoP editor after inspecting instances and project state; `HiBoP` is a project selector, not a verified live instance ID. The host must run the static gate before Unity tests, call `run_tests`, poll `get_test_job` to completion, and inspect console errors before and after. There is no batchmode fallback. Unity compilation and domain reload, completed job identity, exact pass/fail/skipped counts, execution and wall time, and console findings belong in each completion report. A stale report, skipped suite, zero selected tests, or a passing helper without production-path evidence cannot satisfy acceptance. Existing `Tools/run-sync-tests.ps1` is a closed-editor CLI entry point and must not be placed in this strategy's checks.

Host checks run with the project root as cwd. The existing static-gate command is `pwsh -NoProfile -File Tools/check-assembly-dependencies.ps1`; `git diff --check` is a supplementary whitespace check. Their raw nonzero exits require host normalization: 1 for a correctable code/policy failure and 2 for an unavailable tool, project, bridge, editor, license, or human prerequisite. Neither command establishes compilation. Unity MCP job completion plus console inspection establishes the declared Unity compilation target. The repository contains existing test assemblies and tier categories, but future task-specific test names and counts are not yet inspectable; each task must gain a focused filter and expected full names before it becomes ready.

`Docs/dev/quest-autonomous/sync/operation-matrix.md` explicitly provides a coverage record for implementation agents to append evidence. It is the sole mutable plan file: append evidence only for demonstrated rows; never weaken or rewrite its D1–D39 contracts. Validation and budget decisions can be recorded in each task's completion report. T00's report is a baseline reference, not an automatic pass for later p95, allocation, main-thread, or physical-device gates. The final T00 smoke lacked the required 100 color, 300 cut, and 100 timeline completions for qualification p95. Physical USB/Wi-Fi and visual/scientific parity remain human evidence.

All repository-relative scopes below apply only to HiBoP. No additional root is granted for editing. If a task proves a change to hbp_core, EEGFormat, or hbp_math necessary, refine that task before starting it and apply that root's own instructions; do not silently expand scope. No task authorizes commits, staging, branch changes, next-task launch, or changes to frozen plan contracts.
