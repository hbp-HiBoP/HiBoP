# T11 - Modality, bulk site and typed configuration coverage

## Status
Draft; ready is false. Freeze on start.

## Objective
Complete ordinary D1–D34 coverage outside long jobs and activity.

## Shared context and dependencies
T10 structural coverage; operation matrix, state and bulk contracts.

## Scope and preserved contracts
Remaining scientific D11–D13/D22–D26, D33 typed transactions and D34 bulk assignments. Large bodies use independent bulk streams; descriptors/barriers remain ordered; family handlers own checkpoints.

## Allowed paths
`Assets/Scripts/HBP/Sync`, `Assets/Scripts/HBP/Data/Module3D`, `Assets/Scripts/HBP/Quest/Runtime`, `Assets/Tests/EditMode/HBP.Sync.Tests`, `Assets/Tests/EditMode/HBP.Transfer.Scene.Tests`, `Assets/Tests/EditMode/HBP.Quest.Anatomy.Tests`, `Docs/dev/quest-autonomous/sync/operation-matrix.md`.

## Out of scope
Generic state dictionary/capture adapter and long jobs.

## Automated acceptance criteria
A1. Affected assemblies compile. A2. Batches prevalidate and apply atomically; typed checkpoint/config applies through existing handlers with targeted effects. A3. 30,000-site/config bulk emission or resume permits independent interactive traffic, one-site presentation remains O(1), and mixed batches take the strongest lock.

## Verification contract
Static gate, focused Fast/SceneFocused MCP tests and relevant qualification-sized measurement only where required; poll jobs and inspect console. A missing large-fixture measurement is unresolved. Exit 0/1/2 as shared.

## Human acceptance
Review large-body responsiveness and scientific modality parity.

## Unresolved decisions
UNRESOLVED: exact modality subrows/files, large fixture, future tests/counts and MCP pilot.

## Completion report
Append demonstrated matrix evidence and report any blocked row; no Git or next-task action.
