# T16 - Single-scene checkpoint reconciliation UX

## Status
Draft; ready is false. Freeze on start.

## Objective
Implement no-merge reconciliation for one common incarnation.

## Shared context and dependencies
T06 authority plus completed handler/checkpoint families; replication and lifecycle contracts.

## Scope and preserved contracts
Checkpoint identity, silent equal reconnect, global Desktop/Quest choice for the one scene, 200 ms delayed progress, bounded cancelable staging, interaction lock, prevalidation, commit/failure, orphan indicator and retry.

## Allowed paths
`Assets/Scripts/HBP/Sync/Scene`, `Assets/Scripts/HBP/Transfer/Scene`, `Assets/Scripts/HBP/Quest/Runtime`, `Assets/Scripts/HBP/UI/Quest`, `Assets/Tests/EditMode/HBP.Transfer.Scene.Tests`, `Assets/Tests/EditMode/HBP.Quest.Anatomy.Tests`, `Docs/dev/quest-autonomous/sync/operation-matrix.md`.

## Out of scope
Three-way merge, persistent offline history and T17 multi-scene UI.

## Automated acceptance criteria
A1. Affected assemblies compile. A2. Either choice converges without source resources; equal identity is silent; offline Desktop close leaves Quest local. A3. Interrupted staging applies nothing, commit failure is explicit/out-of-sync, retry works and no state survives process death by contract.

## Verification contract
Static gate then focused SceneFocused MCP tests with fake loss/staging; poll job, inspect console and exact results. Human UX review is separate. Exit 0/1/2 as shared.

## Human acceptance
Review delayed progress, choice and orphan/out-of-sync UX.

## Unresolved decisions
UNRESOLVED: `completed handler/checkpoint families` is an unnamed predecessor set in the plan; determine exact prerequisite outputs without changing the original dependency wording. UI/prefab scope, tests/counts and MCP pilot remain open.

## Completion report
Report choice/commit failure boundaries and manual UX; no Git or next-task action.
