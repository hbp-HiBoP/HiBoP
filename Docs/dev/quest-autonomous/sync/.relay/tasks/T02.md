# T02 - Resource manifest and initial-transfer performance repair

## Status
Draft; ready is false. Freeze on start.

## Objective
Give prepared resources stable bounded identities and verified fingerprints without delaying click-to-visible transfer.

## Shared context and dependencies
T01 contracts, T00 baseline, transport performance and scene lifecycle.

## Scope and preserved contracts
Initial/full delivery remains separate from live mutations. Hash immutable buffers on workers or the exact serialization stream; verify on receive. Preserve first-byte/first-visible telemetry and Quest presentation.

## Allowed paths
`Assets/Scripts/HBP/Transfer/Scene`, `Assets/Scripts/HBP/Quest/Runtime/QuestAnatomyView.cs`, `Assets/Scripts/HBP/Quest/Runtime/QuestAnatomyView.cs.meta`, `Assets/Tests/EditMode/HBP.Transfer.Scene.Tests`, `Docs/dev/quest-autonomous/sync/operation-matrix.md`.

## Out of scope
Live-operation scheduling and new heavy incremental resources.

## Automated acceptance criteria
A1. Transfer/Quest runtime and focused transfer tests compile. A2. Production receive rejects fingerprint mismatch and tests show no dedicated second resource traversal. A3. Regression measurements meet T00 transfer budget; duplicate surface/MEG work and per-frame `columns.ToArray()` are removed where still present.

## Verification contract
Static gate precedes task-focused `Sync.SceneFocused` EditMode MCP tests. Poll completed job, inspect console, retain fresh timing and transfer evidence; skipped/zero tests fail. The physical or click-to-visible measurement is a separate manual gate and cannot be passed by unit tests. Exit normalization: 0/1/2 as shared.

## Human acceptance
Review paired transfer performance and first-visible evidence against T00, including unavailable measurements.

## Unresolved decisions
UNRESOLVED: exact fingerprint ownership, qualifying fixture, future tests and measurement availability must be fixed before launch.

## Completion report
List production verification hooks, traversals/copies, tests, measurements and limits; no Git or next-task action.
