# T00 - Baseline instrumentation and fast-test foundation

## Status
Draft. Precompleted only after setup-baseline approval; Relay must not launch it.

## Objective
Preserve the baseline instrumentation, fast tiers, static architecture gate and provisional budgets established before Relay.

## Shared context and dependencies
No predecessor. Read the approved T00 report and verification contract.

## Scope and preserved contracts
No new T00 implementation is authorized. Preserve the original causal milestones, trace/attempt identity, bounded capture, inactive path, and distinction between smoke observations and qualified p95.

## Allowed paths
None; this task is retained as a baseline record.

## Out of scope
Rerunning T00 in Relay or treating prior results as Relay evidence.

## Automated acceptance criteria
A1. The user's prior implementation and tests are recorded as an assertion, not a new compilation result. A2. Keep the original T00 acceptance in the approved backlog. A3. Preserve unavailable device identity, scientific-stable and qualification p95 limitations.

## Verification contract
No Relay check or Unity test is scheduled for T00. Prior evidence is in `T00-baseline-and-budgets.md`; its historical run IDs and limitations remain historical. Host adapter exit codes are 0 pass, 1 correctable failure, 2 environment unavailable, but no adapter is invoked here.

## Human acceptance
Approve the setup baseline that trusts the user's precompletion assertion.

## Unresolved decisions
UNRESOLVED: baseline approval is a human gate; it is not inferred from code presence.

## Completion report
Record only the precompletion assertion and historical evidence reference. No commit, push, staging, branch change, or next-task launch is authorized.
