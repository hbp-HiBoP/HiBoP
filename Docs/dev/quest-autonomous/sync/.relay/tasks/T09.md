# T09 - Selection, appearance and timeline coverage

## Status
Draft; ready is false. Freeze on start.

## Objective
Implement the plan's nonstructural safe D-family coverage.

## Shared context and dependencies
T08 real-scene path; operation matrix, state and timeline contracts.

## Scope and preserved contracts
D1–D2, D5, visual D11–D13, D19, D21, safe D23–D24/D26 and D27–D31, plus selected ROI sphere. Each family gains DTO, handler and typed checkpoint together; local presentation stays local.

## Allowed paths
`Assets/Scripts/HBP/Sync`, `Assets/Scripts/HBP/Data/Module3D`, `Assets/Scripts/HBP/Quest/Runtime`, `Assets/Tests/EditMode/HBP.Sync.Tests`, `Assets/Tests/EditMode/HBP.Transfer.Scene.Tests`, `Assets/Tests/EditMode/HBP.Quest.Anatomy.Tests`, `Docs/dev/quest-autonomous/sync/operation-matrix.md`.

## Out of scope
Structural families, long-job calculation and Quest product controls.

## Automated acceptance criteria
A1. Affected assemblies compile. A2. Focused Desktop and Quest-driver tests per applicable row prove payload, ordering, invalidation, echo suppression, checkpoint and local exclusion. A3. Timeline invalid/stale clock, asymmetric latency and long uptime are tested; correction only beyond one sample.

## Verification contract
Static gate, affected `Sync.Fast` then `Sync.SceneFocused` MCP filters; poll completion, inspect console, require explicit per-row names/counts and timings. Exit 0/1/2 as shared.

## Human acceptance
Review row classification and local presentation exclusion.

## Unresolved decisions
UNRESOLVED: exact safe subrow boundaries, files, named tests/counts and clock-quality thresholds need evidence before launch.

## Completion report
Append demonstrated matrix evidence only; report closure rows and limitations; no Git or next-task action.
