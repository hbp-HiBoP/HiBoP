# T08 - Real-scene v2 vertical slice, publication journal and cutover

## Objective
Activate color/cut/timeline v2 on a real prepared Desktop/Quest scene and prove initial-publication replay.

## Shared context and dependencies
T02, T04–T07 outputs; T00 budgets, scene lifecycle and integration contracts.

## Scope and preserved contracts
One active owner per scene; off-thread decode, early single-point Quest apply, bounded journal from capture boundary through publication, ordered replay or one checkpoint fallback, topology-abort/restart and wrapper preservation.

## Out of scope
Broad D1–D34 coverage and physical qualification substituted by synthetic tests.

## Automated acceptance criteria
A1. Affected runtime/test assemblies compile. A2. Both directions work without snapshot capture; in-transfer mutation/structural replay, overflow fallback, topology abort and no double owner are demonstrated. A3. T00 color/cut/timeline p95, GC and main-thread budgets receive explicit pass/fail decisions from qualifying evidence; missing counts remain unavailable, not pass.

## Verification contract
Static gate, focused `Sync.SceneFocused` MCP tests, completed job and console inspection. Preserve separate fresh performance records with 100/300/100 qualifying completions where p95 is claimed.

## Human acceptance
Inspect real scene, Quest wrapper retention, and paired budget evidence; unavailable physical evidence remains open.

## Unresolved decisions
UNRESOLVED: exact live-entry files, qualification fixture/device availability, test names/counts.

## Completion report
Report each budget as pass/fail/unavailable with trace provenance and closure table.
