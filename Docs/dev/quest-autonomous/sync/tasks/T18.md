# T18 - Legacy removal and full qualification

## Objective
Remove dead experimental sync architecture and establish the intended release gates.

## Shared context and dependencies
All tasks required by the intended release; T00 baseline, matrix and verification contracts.

## Scope and preserved contracts
Delete unused snapshot/delta/merge/hot-capture/session code and obsolete tests only after proving inactivity. Complete D1–D34 evidence, fast/integration shards, slow real-scene and physical USB/Wi-Fi qualification. Preserve initial transfer and inactive Desktop gates.

## Out of scope
Compatibility shims kept without a demonstrated active need, speculative unrelated cleanup and silently relaxing release gates.

## Automated acceptance criteria
A1. All affected assemblies compile. A2. Applicable D1–D34 rows pass both driver directions; no active compatibility shim or stale job publish remains. A3. Fast/integration and slow qualification evidence, inactive Desktop and initial-transfer regression gates pass; any unavailable physical result remains manual/unresolved.

## Verification contract
Static gate, named focused and qualification MCP EditMode jobs, each polled to completion with console/errors, counts, timing and fresh run identity. Physical USB/Wi-Fi milestones require separate recorded human/device evidence and cannot be inferred from MCP tests.

## Human acceptance
Inspect USB/Wi-Fi latency, visual/scientific parity, native versions, fixture and profiler setup, and release limitations.

## Unresolved decisions
UNRESOLVED: the plan says `all tasks required by the intended release`; identify that set before implementation. Exact dead-file list, named tests, device access and qualified T00 comparison remain open.

## Completion report
Give complete acceptance-to-evidence table, changed/deleted files, measurements and limitations.
