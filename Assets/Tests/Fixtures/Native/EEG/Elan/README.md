Convert `../BrainVision/conversion_seed_elan.vhdr` with the internal C++ tool
and save the result here as `native_from_brainvision.eeg` plus the associated
`native_from_brainvision.eeg.ent`, `.pos` and optional `.txt` files.
